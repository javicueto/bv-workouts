# The coach's exercise videos stay OFF GitHub: the repo is public, they are not
# ours to redistribute, and they are already on YouTube — which is what the site
# links to when you tap a preview. They live in Google Drive as the backup.
videos/

# Poster frames pulled from those videos. Superseded by previews/ and only used
# by the offline local copy.
thumbs/

# previews/ is NOT ignored on purpose. Those short looping WebPs are what the
# site shows on every card, so they must be published.

# Local run logs
logs/

.DS_Store

# Look-and-feel mockups (the two Court/Programme directions, the card-shade
# picker). Kept on this Mac as the record of what was compared; NOT in the
# public repo and NOT on the published site (Javier, 12 Sep 2026).
tests/

# PRIVATE — never in the public repo (Javier, 12 Sep 2026). The project notes
# hold account and security facts, the archive holds a friend’s personal
# details, and _backup/ holds the pre-scrub history. All three live only on
# Javier’s Drive; the notes and archive were also scrubbed from git history.
CLAUDE.md
CLAUDE.local.md
_archive/
_backup/
